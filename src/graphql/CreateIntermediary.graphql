mutation CreateIntermediary($input: PublicCreateIntermediaryInput) {
    publicCreateIntermediary(input: $input) {
        intermediary {
            cocNumber
            createdAt
            id
            name
            token
            updatedAt
        }
    }
}
