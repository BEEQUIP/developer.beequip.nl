---
title: Queries
description: An overview of GraphQL queries in the marketplace API.
---

# Queries

An overview of GraphQL queries that can be used to fetch data.

### canObjectBePlaced

Returns whether the given object satisfies all conditions for placement on the marketplace. Call this query before using the mutations `addObject` or `updateObject`.

### categoryGroups

Returns a list of category groups and their categories.
